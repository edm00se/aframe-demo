{
  "name": "aframe-demo",
  "version": "1.0.0",
  "description": "a demo of the aframe framework for Google Cardboard, etc.",
  "scripts": {
    "dev": "npm run build && serve",
    "build": "browserify js/main.js | uglifyjs > js/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eric McCormick (https://ericmccormick.io/)",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^14.5.0",
    "serve": "^6.4.1",
    "uglify-js": "^3.2.0"
  },
  "dependencies": {
    "aframe": "^0.7.1"
  }
}

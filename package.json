{
  "name": "aframe-demo",
  "version": "1.0.0",
  "description": "a demo of the aframe framework for Google Cardboard, etc.",
  "scripts": {
    "dev": "npm run build && lite-server -c .bs-config.js",
    "build": "browserify js/main.js -o js/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eric McCormick (https://ericmccormick.io/)",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^14.5.0",
    "lite-server": "^2.2.2"
  },
  "dependencies": {
    "aframe": "^0.7.1"
  }
}
